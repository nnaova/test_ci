name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build, Lint and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0' # adapte à ta version
          
      - name: Install dependencies
        run: flutter pub get
        
      - name: Run linter (flutter analyze)
        run: flutter analyze
        
      - name: Run tests
        run: flutter test
